" Source this file early, before any plugins add mappings.

" Use space as leader, use thumbs rather than pinky
let mapleader=' '
let maplocalleader=' '
" Keep backslash as a leader, for muscle memory
map \ <Leader>
